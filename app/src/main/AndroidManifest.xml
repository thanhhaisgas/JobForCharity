<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.drowsyatmidnight.jobforcharity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.LoginActivity"
            android:theme="@style/loginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home">

        </activity>
        <activity android:name=".login.SignUpActivity"
            android:theme="@style/signupActivity"/>
        <activity
            android:name=".userhire.Home_UserHire"
            android:label="@string/title_activity_home__user_hire"
            android:theme="@style/fragment"></activity>

        <activity android:name=".hirer.View.Acitivities.CreateWorkActivity"
            android:theme="@style/joblistActivity"
            />

        <activity android:name=".hirer.View.Acitivities.NeededWorksMngmntActivity"
            android:theme="@style/NeededWorksMngmntActivity"
            />
    </application>

</manifest>